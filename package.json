{
  "name": "gas-tweet-archive",
  "version": "0.1.0",
  "description": "X(Twitter)データアーカイブをGoogleスプレッドシートで可視化・分析するツール",
  "private": true,
  "scripts": {
    "build": "tsc && cp -r src/pages/*.html dist/ && cp appsscript.json dist/",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf dist",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/google-apps-script": "^1.0.83",
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.4.0"
  }
}
